#include <iostream>

using namespace std;

#define MAX 1000;

int fall[MAX];

//tree->timeの時にどちらの木にいるか
int solve(int time, bool change, int W, int tree)
{
    if (change)
        W--;
}

int main()
{
    int T, W;
    cin >> T >> W;
}